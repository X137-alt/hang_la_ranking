# 控制自动化考研院校难度精细排名

GitHub Pages 静态网页版本，参考学校 Logo 拖拽排名工具的交互形式，主题改为控制/自动化考研院校难度精细排名。

## 目录结构

```text
school_logo_ranking/
├─ index.html
├─ style.css
├─ script.js
├─ README.md
├─ .nojekyll
└─ assets/
   └─ logos/
      ├─ 清华大学.png
      ├─ 浙江大学.png
      └─ ...
```

## 本版调整

- 院校卡片只显示 Logo 和院校名称。
- 移除了页面内单独的“展示缩放”按钮，整体大小跟随浏览器 Ctrl+滚轮 / 触控板缩放。
- 六栏排名面板使用等分响应式布局，浏览器缩放或窗口宽度变化时会自动重新计算卡片位置。
- 保留拖拽、自动保存、重置排名、自动整理、导出排名、直播模式、全屏展示。
- 底部说明卡片已移除。

## 使用方法

1. 把院校 Logo 放到 `assets/logos/`。
2. Logo 文件名使用院校名称，例如 `清华大学.png`。
3. 将本目录里的文件上传到 GitHub 仓库根目录。
4. 在 GitHub Pages 中选择 `main` 分支和 `/(root)` 发布。


## 本版更新

- Logo 图片在卡片中更大比例填充，候选区和排名区更清晰。
- 增加卡片呼吸灯、候选区柔光边框等动态效果。
- 六档颜色按难度从强到弱重新配色：红、橙、黄、绿、蓝、灰。
- 保持浏览器原生缩放逻辑，Ctrl + 鼠标滚轮即可整体缩放页面。


## 本次调整

- 六栏标题删除副标题小字，仅保留大字。
- 右侧提示文案改为“越靠右越拉”。
- 保留 Logo 填充、呼吸灯和自动保存等功能。


## 本次版式说明

- 待评价区改为单行横向滚动展示。
- 院校 Logo 卡片略微缩小，并保留轻微呼吸灯效果。
- 部分院校展示简称已按用户要求调整。


## 本版调整

- 排名面板每个档位栏位已按“至少横向容纳 3 个院校 Logo”重新计算。
- 待评价区保持多行排布、单行高度展示，并通过上下滚动切换可视行。
- 自动整理会按每栏至少 3 列进行布局，减少同一档位内 Logo 拥挤。


## 本版布局说明

排名区 Logo 卡片已按每个档位约可横向容纳 3.5 个 Logo 的比例调整，便于每栏展示 3 个完整院校并保留半个卡片的视觉缓冲。


## 本版更新：顺滑精准拖拽

- 拖拽过程使用 `requestAnimationFrame + transform: translate3d`，减少 left/top 布局重排。
- 拖动时暂停呼吸灯动画，减少拖拽卡顿。
- 鼠标/触控点作为 Logo 中心定位，放置时更容易对准目标位置。
- 保存坐标精度提升到两位小数，细微位置更稳定。


## 本版拖拽优化

- 鼠标按住 Logo 的哪个位置，拖动时该点会保持贴住鼠标。
- 使用 fixed + translate3d 合成层移动，避免 left/top 布局重排造成延迟。
- 命中检测与占位更新放到动画帧中，减少拖动卡顿。
